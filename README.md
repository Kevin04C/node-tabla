# Notas

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base para la tabla de multiplicar     [number] [required]
  -l, --listar   Muestra la tabla por la consola      [boolean] [default: false]
  -h, --hasta    Cantidad n números que será multiplicada por la base
                                                          [number] [default: 10]
```
